<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.neu.madcourse.numad21su_emilycolladay">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_ec"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_ec_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NUMAD21SuEmilyColladay">
        <activity
            android:name=".ButtonClick"
            android:configChanges="orientation|screenSize|keyboardHidden" />

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LettersClick"
            android:configChanges="orientation|screenSize|keyboardHidden" />
    </application>

</manifest>